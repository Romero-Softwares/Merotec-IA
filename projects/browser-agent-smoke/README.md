# Teste agente e navegador

Projeto mínimo sem dependências para verificar que o agente consegue servir uma página, abri-la no navegador interno e executar uma interação real.

Execute na pasta do projeto:

```powershell
python -m http.server 8765
```

Depois acesse `http://127.0.0.1:8765/`.
